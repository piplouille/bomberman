#include "player.hpp"
#include "map.hpp"

int main () {
    Player maxence(0);
    Map map;

    // on va mettre maxence sur bloc (0, 0)

    // il faut thread dédié en while(1)
    // inline void Player::move(char move){


    return 0;
}